# Supply-chain guard: only install package versions published at least 7 days
# ago, so freshly compromised releases (usually pulled within hours/days) never
# get resolved. Enforced by npm >= 11.6; older npm ignores it with a warning.
# Exempt a package with min-release-age-exclude if an urgent fix is needed.
min-release-age=7
